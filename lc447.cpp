#include <cmath>
#include <iostream>
#include <vector>

using namespace std;

int numberOfBoomerangs(vector<vector<int>>& points);
int distanceSquared(vector<int>& point1, vector<int>& point2);

int main() {}

int numberOfBoomerangs(vector<vector<int>>& points) {}

int distanceSquared(vector<int>& point1, vector<int>& point2) {
  return (point1[0] - point2[0]) * (point1[0] - point2[0]) +
         (point1[0] - point2[0]) * (point1[0] - point2[0]);
}
